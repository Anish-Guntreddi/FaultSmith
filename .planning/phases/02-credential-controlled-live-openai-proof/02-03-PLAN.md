---
phase: 02-credential-controlled-live-openai-proof
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/live-smoke.mjs
  - scripts/live-smoke.test.mjs
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04]
must_haves:
  truths:
    - Default invocation proves fallback and makes no paid call.
    - Live invocation requires `--live`, refuses a server reporting live false, and never reads or prints a key.
    - CLI failures are non-zero and safe; evidence is opt-in and contained.
  artifacts:
    - Thin tested operator CLI over the shared lifecycle core.
---

<objective>
Expose the shared lifecycle as safe default-offline and explicit-live operator commands.
</objective>

<tasks>
<task id="02-03-01" type="auto">
  <name>Implement explicit offline/live smoke CLI</name>
  <files>scripts/live-smoke.mjs</files>
  <action>Parse `--base-url`, optional `--live`, optional `--evidence`, and `--help` with no dependency. Default to loopback offline/fallback mode. Resolve the current Git SHA without embedding command output in evidence errors, call the shared lifecycle, print a concise safe stage table/summary, and optionally write evidence only under `test-results/`. Never read, accept, or echo an API key argument. Live mode must stop after health if the server reports live false.</action>
  <verify>`node --check scripts/live-smoke.mjs`; CLI tests invoke `main()`/argument parsing against fake-server URLs.</verify>
  <done>The eventual key-backed proof is one explicit command while today's default remains free and safe.</done>
</task>
<task id="02-03-02" type="auto">
  <name>Test CLI safety and process semantics</name>
  <files>scripts/live-smoke.test.mjs</files>
  <action>Cover help, unknown/missing arguments, default offline mode, explicit live mode, health false short-circuit, evidence opt-in, invalid evidence path, sanitized stdout/stderr, and non-zero failures using fake-server lifecycle fixtures. Assert neither CLI arguments nor output can contain a credential field/value.</action>
  <verify>`npx vitest run scripts/live-smoke.test.mjs`; `npm run security:source`.</verify>
  <done>Operator error cannot accidentally trigger paid behavior or leak sensitive payloads.</done>
</task>
</tasks>

<verification>
- CLI focused tests and core tests pass together.
- No normal package quality command invokes `--live`.
</verification>

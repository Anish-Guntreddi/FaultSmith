---
phase: 02-credential-controlled-live-openai-proof
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - .planning/phases/02-credential-controlled-live-openai-proof/02-LIVE-EVIDENCE.md
  - docs/BUILD_LOG.md
  - docs/COMPLETION_REPORT.md
  - docs/ROADMAP.md
  - docs/TESTING.md
  - docs/THREAT_MODEL.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04]
must_haves:
  truths:
    - Real GPT-5.6, Code Interpreter, live hint, and score-only assessment behavior is proven on a named reviewed SHA.
    - A failing patch remains not verified and every provider failure still preserves labeled fixture recovery.
    - No key, raw provider response, code, hint/prose, output, provider/container ID, or hidden answer appears in evidence or Git.
  artifacts:
    - Sanitized live evidence, rerun gates, updated canonical/GSD proof.
---

<objective>
At the final private credential checkpoint, run and review the real provider proof before any deployment.
</objective>

<tasks>
<task id="02-06-01" type="auto">
  <name>Verify reviewed offline head and credential absence</name>
  <files>none</files>
  <action>Confirm branch SHA/clean sync, four CI checks, full local gate, fallback production smoke, and that health currently reports live false. Do not inspect, request, or print a credential value.</action>
  <verify>All Phase 2 offline plans have summaries and gates are green.</verify>
  <done>The exact offline-reviewed head is ready for private server configuration.</done>
</task>
<task id="02-06-02" type="checkpoint:human-action">
  <name>Configure the server-only key privately</name>
  <action>Ask the user to place `OPENAI_API_KEY` in ignored root `.env.local`, verify it is funded/authorized, start the reviewed production build on loopback, and confirm only that `/api/health` reports `liveOpenAIConfigured: true`. Explicitly instruct the user not to paste the key into chat or terminal output.</action>
  <verify>User replies that the reviewed local server health reports live configured.</verify>
  <done>Live server is ready without credential disclosure.</done>
</task>
<task id="02-06-03" type="auto">
  <name>Run live proof, adversarial review, self-heal, and final gates</name>
  <files>02-LIVE-EVIDENCE.md and canonical/GSD evidence files</files>
  <action>Run the explicit live smoke against loopback with evidence under ignored test-results. Verify exact generated contract, the reviewed workflow proof chain that permits `source: generated` only after original-pass, mutated/repaired Code Interpreter evidence, live hint, score-only assessment, failing-patch authority, and non-disclosing logs/artifacts. Actual provider evidence proves LIVE-01–03; LIVE-04 combines the actual no-key route smoke with named malformed-output, timeout, expiration, and failure-injection regressions on the same runtime implementation SHA. Independently review safe evidence. Record both the live-tested runtime SHA and the later evidence-only commit SHA; any runtime repair creates a new SHA and requires the live proof again. For any failure, record only safe stage/rule, add a mocked regression, repair, and rerun affected gates; do not weaken fixture fallback.</action>
  <verify>LIVE-01–03 map to actual provider evidence on the runtime SHA; evidence contains only allowlisted facts.</verify>
  <done>Live runtime evidence is captured safely and no runtime file changes after the tested SHA.</done>
</task>
<task id="02-06-04" type="checkpoint:human-action">
  <name>Stop the live server and remove the private credential</name>
  <action>The user stops the credentialed server and privately removes `OPENAI_API_KEY` from `.env.local` (or removes the ignored file). The agent must not inspect, print, edit, or delete the credential.</action>
  <verify>The user confirms only that cleanup is complete; a new server reports `liveOpenAIConfigured: false`.</verify>
  <done>The workspace is safe for repository-wide source/history scanning and fallback proof.</done>
</task>
<task id="02-06-05" type="auto">
  <name>Finalize evidence-only documentation and rerun gates</name>
  <files>02-LIVE-EVIDENCE.md and canonical/GSD evidence files</files>
  <action>Run the no-key fallback smoke, named provider-failure regressions, source/history scan, full quality gate, and audit. Update only evidence/document files with the live-tested runtime SHA and evidence commit SHA, push, wait for four required checks, and keep the PR draft/unmerged. If runtime code must change, return to the offline checkpoint and repeat the live proof.</action>
  <verify>LIVE-01–04 map to objective evidence; both SHAs are recorded; full gates and required CI are green; no credential is committed.</verify>
  <done>Phase 2 is complete locally and Phase 3 is the next deployment-approval gate.</done>
</task>
</tasks>

<verification>
- Actual provider evidence satisfies LIVE-01–03; actual no-key smoke plus named deterministic provider-failure regressions satisfy LIVE-04.
- Deployment has not occurred.
</verification>

---
phase: 02-credential-controlled-live-openai-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/submission-readiness.mjs
  - scripts/submission-readiness.test.mjs
  - docs/UAT_PROTOCOL.md
  - docs/uat-results.template.json
autonomous: true
requirements: [LIVE-03, LIVE-04]
must_haves:
  truths:
    - Submission preparation can be validated now without inventing live, deployment, tester, video, or feedback evidence.
    - UAT results exclude learner code/prose and cannot claim the five-person/4-of-5 threshold without valid bounded records.
    - Strict final mode fails on placeholders and missing external links/evidence while preparation mode reports them as pending.
  artifacts:
    - Tested submission/UAT validator, protocol, and safe template.
---

<objective>
Prepare and validate the human/submission package without fabricating external results.
</objective>

<tasks>
<task id="02-02-01" type="auto">
  <name>Implement submission and UAT readiness validation</name>
  <files>scripts/submission-readiness.mjs, scripts/submission-readiness.test.mjs</files>
  <action>Create an importable CLI/module using built-ins only. Validate a versioned UAT JSON shape with exactly bounded anonymous tester IDs, completion, purpose-understood boolean, duration, blocker/high findings, and no learner code/prose/email/name fields. Compute—but never manufacture—the 5-person and 4-of-5 thresholds. Inspect submission/demo/README text for required repository, video, demo/test-build, feedback ID, tester result, license, Codex/GPT disclosure, and placeholder markers. Default `--prepare` reports pending gates and exits zero; `--strict` exits non-zero until all external evidence is valid. Errors print stable rule IDs and paths, not sensitive values.</action>
  <verify>`npx vitest run scripts/submission-readiness.test.mjs`; CLI fixtures cover prepare/strict, malformed/duplicate/undersized results, learner-text rejection, and placeholder detection.</verify>
  <done>Readiness can be assessed honestly before and after external evidence using one versioned validator.</done>
</task>
<task id="02-02-02" type="auto">
  <name>Create the tester protocol and safe results template</name>
  <files>docs/UAT_PROTOCOL.md, docs/uat-results.template.json</files>
  <action>Document a repeatable five-person test: consent-neutral anonymous IDs, no extra purpose explanation before the comprehension question, primary guided task, timing, observed blockers, severity/reproduction, and retest policy. Provide a syntactically valid template that intentionally remains incomplete/pending and contains no person data, source, hypothesis, or explanation. Explain that only actual observations may replace placeholders and that blocker/high findings return through regressions and retest before recording.</action>
  <verify>Validator accepts the template only in prepare mode and rejects it in strict mode; source scan remains clean.</verify>
  <done>User can run UAT immediately after deployment without redesigning data collection or risking private learner content.</done>
</task>
</tasks>

<verification>
- Preparation mode is green now; strict mode demonstrably fails for the right pending external gates.
- No user result is represented as completed.
</verification>

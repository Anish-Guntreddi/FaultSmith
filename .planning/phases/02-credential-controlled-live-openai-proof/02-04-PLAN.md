---
phase: 02-credential-controlled-live-openai-proof
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/production-smoke.mjs
  - scripts/production-smoke.test.mjs
  - docs/DEPLOYMENT.md
autonomous: true
requirements: [LIVE-04]
must_haves:
  truths:
    - The same production check runs against loopback now and an approved HTTPS host later.
    - Security/cache/health/fallback claims are asserted mechanically.
    - Deployment remains approval-gated; edge limiting and CSP feasibility remain explicit target decisions.
  artifacts:
    - Tested production smoke CLI and Vercel-compatible deployment/rollback runbook.
---

<objective>
Prepare target-neutral production verification and a precise no-deploy runbook.
</objective>

<tasks>
<task id="02-04-01" type="auto">
  <name>Implement production root, header, health, and lifecycle smoke</name>
  <files>scripts/production-smoke.mjs, scripts/production-smoke.test.mjs</files>
  <action>Create a built-in-only CLI/module accepting base URL and expected mode. Check unauthenticated root/health HTTP 200, `no-store` API caching, CSP, HSTS on HTTPS expectations, frame denial, nosniff, referrer/permissions/opener/resource policies, absent `X-Powered-By`, health truth, and the shared offline/live lifecycle. Permit loopback HTTP only. Use fake-server tests for every missing/bad header, auth redirect, cache drift, status/mode drift, and safe non-zero output.</action>
  <verify>`npx vitest run scripts/production-smoke.test.mjs`; `node --check scripts/production-smoke.mjs`.</verify>
  <done>Local production and later public deployment use one repeatable non-disclosing smoke contract.</done>
</task>
<task id="02-04-02" type="auto">
  <name>Write Vercel-compatible deployment and rollback runbook</name>
  <files>docs/DEPLOYMENT.md</files>
  <action>Document exact preconditions, reviewed SHA, `npm ci/build/start`, server-only environment setup, Vercel compatibility without claiming deployment, 30s/20s ordering, preview/public auth requirements, edge/shared limiter decision before public paid use, CSP nonce/hash feasibility decision, health/security/cache/live/fallback/viewport smoke, rollback by removing the key to preserve fixture-only function, evidence manifest fields, and availability-through-judging checks. Explicitly require user approval before any host mutation.</action>
  <verify>Claim tests/source scan pass; every DEP requirement is mapped to a future command/evidence field rather than marked complete.</verify>
  <done>After local live proof, deployment is an operator action plus mechanical verification—not new engineering discovery.</done>
</task>
</tasks>

<verification>
- Production smoke fake-server suite covers every header and lifecycle rule.
- Runbook contains no credentials/account-specific assumptions.
</verification>

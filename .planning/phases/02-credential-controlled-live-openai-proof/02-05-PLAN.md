---
phase: 02-credential-controlled-live-openai-proof
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - package.json
  - README.md
  - docs/BUILD_LOG.md
  - docs/DEMO_SCRIPT.md
  - docs/ROADMAP.md
  - docs/SUBMISSION.md
  - docs/TESTING.md
  - docs/THREAT_MODEL.md
  - .planning/STATE.md
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04]
must_haves:
  truths:
    - Operators have documented npm commands for offline, explicit live, production, and submission readiness.
    - A real production offline lifecycle passes before any credential is requested.
    - Canonical docs distinguish built tooling from still-unverified provider/deployment/human evidence.
  artifacts:
    - Package scripts, canonical docs, offline production evidence, and Phase 2 credential handoff.
---

<objective>
Integrate the tools, prove the complete no-key production path, update canonical evidence, and reach the credential checkpoint with every offline gate green.
</objective>

<tasks>
<task id="02-05-01" type="auto">
  <name>Wire safe release-readiness commands</name>
  <files>package.json</files>
  <action>Add clearly named scripts for fallback smoke, explicit live smoke, production smoke, and submission prepare/strict audit. Ensure `npm run quality` and GitHub CI never invoke the live flag or require a credential. Keep no new dependency.</action>
  <verify>`npm run`; focused CLI tests; inspect CI/package command graph for `--live` absence.</verify>
  <done>All commands are discoverable and paid behavior remains opt-in.</done>
</task>
<task id="02-05-02" type="auto">
  <name>Run production fallback smoke and full downstream gates</name>
  <files>test-results only (ignored)</files>
  <action>Build and start the exact branch production server on loopback with no key. Run fallback smoke with safe evidence, production header/health/lifecycle smoke, and submission prepare audit. Then run source/history, lint, typecheck, all Vitest, build, bundle, Playwright/axe, and moderate audit. Fix any in-scope failure with a regression and rerun affected downstream gates; preserve fallback. Commit and push the clean reviewed offline checkpoint, then wait for all four required GitHub checks before requesting the private credential.</action>
  <verify>`npm run security:source && npm run quality && npm audit --audit-level=moderate`; new smoke/readiness commands green in expected modes.</verify>
  <done>Every credential-free route, operator tool, security gate, and learner workflow is green on one SHA, and that exact offline checkpoint has four green required GitHub checks.</done>
</task>
<task id="02-05-03" type="auto">
  <name>Update canonical release and submission guidance</name>
  <files>README.md, docs/BUILD_LOG.md, docs/DEMO_SCRIPT.md, docs/ROADMAP.md, docs/SUBMISSION.md, docs/TESTING.md, docs/THREAT_MODEL.md, .planning/STATE.md</files>
  <action>Document exact commands/results, official rule access requirement, safe evidence schema, subscription/API billing distinction, explicit live flag, production runbook, UAT protocol, remaining credential/deployment/human gates, and rollback to labeled fixture mode. Do not claim live/provider, deployment, UAT, or video evidence. Advance state only to the Phase 2 credential checkpoint.</action>
  <verify>Claim regression, source scan, link/placeholder preparation audit, `git diff --check`, and cross-document status search.</verify>
  <done>The user can configure a key privately and run one local proof without further build/design work.</done>
</task>
</tasks>

<verification>
- Full local gate and production fallback smoke pass.
- Submission strict audit fails only for named external evidence; preparation audit passes.
</verification>

---
phase: 02-credential-controlled-live-openai-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/release-smoke-core.mjs
  - scripts/release-smoke-core.test.mjs
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04]
must_haves:
  truths:
    - One route-level lifecycle implementation can prove labeled fallback now and exact live behavior later without direct SDK calls.
    - Non-loopback HTTP, URL credentials/query/fragment/path drift, redirects, hidden or unknown fields, unsafe response bodies, mode/source drift, evidence promotion, and timeouts fail closed with stable rule IDs.
    - Safe evidence contains only versioned public facts, counts, modes, sources, hashes, SHA, origin, and timestamps—never code, hints, prose, outputs, credentials, provider IDs, or hidden answers.
  artifacts:
    - Tested shared smoke/evidence module with no new dependency.
  key_links:
    - The harness exercises the same public Next routes judges use; it cannot bypass workflow policy.
---

<objective>
Build the credential-safe shared HTTP lifecycle and evidence core used by offline, live, and production smoke commands.
</objective>

<tasks>
<task id="02-01-01" type="auto">
  <name>Implement safe URL, request, assertion, lifecycle, and evidence primitives</name>
  <files>scripts/release-smoke-core.mjs</files>
  <action>Create an importable Node ESM module using built-ins only. Enforce HTTPS except loopback HTTP; reject URL credentials, query, fragment, unexpected base paths, and redirects; normalize origins; use bounded AbortSignal requests; parse JSON without including bodies in exceptions; validate every route response with exact key allowlists, types, bounds, arrays, and unknown-field rejection; recursively reject hidden/internal/provider/credential fields; apply the exact primary one-line repair exactly once without serializing it; and implement fallback/live lifecycle assertions for health, generate, hint, mutated execute, repaired execute, repaired assess, and failing assess. The no-key lifecycle must deliberately request `code_interpreter` once and require `fallbackUsed: true`, `recoveredFrom: missing_key`, and `prevalidated_fixture`. Return a versioned evidence object containing repository SHA supplied by caller, mode, origin, timestamp, safe stage facts/counts/source/mode, the explicit reviewed-workflow proof-chain fact for original-pass gating, and SHA-256 output digests only. Provide an explicit safe JSON writer that rejects paths outside a caller-approved evidence directory, symlink traversal, and existing-target overwrite using no-follow/exclusive creation; never write by default.</action>
  <verify>Focused tests in task 02-01-02 exercise each exported policy; `node --check scripts/release-smoke-core.mjs`.</verify>
  <done>The core supports both expected modes while failing closed and never reading an API key.</done>
</task>
<task id="02-01-02" type="auto">
  <name>Adversarially test the core with fake HTTP lifecycles</name>
  <files>scripts/release-smoke-core.test.mjs</files>
  <action>Use Vitest plus an in-process Node HTTP server. Cover complete fallback/live success, live flag with health false before POST, non-loopback HTTP rejection, URL credentials/query/fragment/path, redirect rejection, abort, non-JSON, HTTP failure without body disclosure, recursive hidden fields, benign extra keys, wrong types/ranges/arrays, source/execution/fallback drift, explicit missing-key Code Interpreter recovery, mutated-pass, repaired-fail, failing-assessment promotion, unsafe hint, zero/duplicate repair transform, stable evidence schema, redaction, digest behavior, path containment, symlink traversal, and exclusive/non-overwriting writes. Assert no test performs an OpenAI/network call outside its loopback fake server.</action>
  <verify>`npx vitest run scripts/release-smoke-core.test.mjs`; `npm run lint`; `npm run typecheck`.</verify>
  <done>Fake-server tests prove the full operator contract without a credential or paid request.</done>
</task>
</tasks>

<verification>
- Focused tests pass and error messages disclose only stage/rule/status.
- Source scan finds no credential or public-secret regression.
</verification>

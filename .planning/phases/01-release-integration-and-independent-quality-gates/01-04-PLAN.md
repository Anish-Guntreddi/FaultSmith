---
phase: 01-release-integration-and-independent-quality-gates
plan: 04
type: review
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/01-release-integration-and-independent-quality-gates/01-04-SECURITY-REVIEW.md
autonomous: true
requirements: []
supports: [DEV-02, DEV-03, SEC-01, SEC-02, SAFE-02]
must_haves:
  truths:
    - Independent adversarial review covers every browser/server/provider trust boundary, secrets/history, dependencies, and evidence authority.
    - No blocker/high issue is omitted or self-repaired by the reviewer.
  artifacts:
    - 01-04-SECURITY-REVIEW.md with threat attempts, results, residuals, and structured findings.
  key_links:
    - Findings feed Plan 05 and update the canonical threat model only in Plan 06.
---

<objective>
Independently adversarially test FaultSmith's security, privacy, answer-containment, and deterministic-authority boundaries at the frozen Wave 1 SHA.
</objective>

<tasks>
<task id="01-04-01" type="auto">
  <name>Audit and attempt trust-boundary violations</name>
  <files>.planning/phases/01-release-integration-and-independent-quality-gates/01-04-SECURITY-REVIEW.md</files>
  <action>Read the baseline manifest and create a unique detached temporary Git worktree at its exact implementation SHA. Run `npm ci` there before auditing strict JSON schemas, sizes/counts/paths, allowlists, arbitrary command/container/repository inputs, prompt injection, hint/reference leakage, public DTO/import/storage/bundle leakage, failing-prose promotion, overbroad repair, output/error sanitation, rate-key churn, source/history credentials, dependencies/licenses, CSP/headers, and host subprocess absence. Run unit/adversarial, audit, source, bundle, and targeted searches in that worktree. Classify per-process limiter and inline CSP residuals in context. Write only the repository-root-qualified owned report. Stop any worktree-local process, then remove only this task's temporary worktree; do not implement fixes.</action>
  <verify>`npm test && npm audit --audit-level=moderate && npm run security:source && npm run security:bundle` in the baseline worktree; report SHA equals manifest; `git diff --name-only <baseline>..HEAD -- src tests scripts package.json .github` is empty, allowing sibling reports/GSD metadata.</verify>
  <done>Every attempt has result/evidence and report explicitly states unresolved blocker/high status.</done>
</task>
</tasks>

<verification>
- Report includes severity, affected files, exploit/failure path, mitigation, and validation for each finding.
- No secret contents are copied into the report.
</verification>

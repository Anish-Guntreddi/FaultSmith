---
phase: 01-release-integration-and-independent-quality-gates
plan: 03
type: review
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/01-release-integration-and-independent-quality-gates/01-03-QA-REVIEW.md
autonomous: true
requirements: []
supports: [DEV-02, DEV-03, QA-02, SAFE-01]
must_haves:
  truths:
    - Independent QA verifies automated gates plus keyboard, axe, visual focus/contrast, responsive, persistence, duplicate/retry, and recovery behavior.
    - Review evidence is bound to the immutable Wave 1 SHA in `01-01-BASELINE.md`.
  artifacts:
    - 01-03-QA-REVIEW.md with automation/manual matrix and structured findings.
  key_links:
    - Findings feed Plan 05; browser review uses the existing app and does not implement fixes.
---

<objective>
Independently test quality, accessibility, responsive layout, persistence, and failure recovery at the frozen Wave 1 SHA.
</objective>

<tasks>
<task id="01-03-01" type="auto">
  <name>Run automated QA and manual browser/accessibility review</name>
  <files>.planning/phases/01-release-integration-and-independent-quality-gates/01-03-QA-REVIEW.md</files>
  <action>Read the baseline manifest and create a unique detached temporary Git worktree at its exact implementation SHA. Run `npm ci` and install Playwright Chromium in that worktree before lint/type/unit/build/bundle/E2E evidence. Inspect that exact app in the in-app browser at 1440×900 and 390×844. Cover keyboard-only flow, visible focus, labels/landmarks/live regions, axe, contrast, reduced motion, primary/guided success and guided failure, refresh/report restoration, reset/new-lab clearing, rapid/duplicate controls, retry/missing-key behavior, overflow, and console/runtime errors. Write commands/results to the repository-root-qualified owned report. Stop the worktree-local server, then remove only this task's temporary worktree. Do not edit application/tests/canonical docs.</action>
  <verify>`npm run test:e2e` in the baseline worktree; report SHA equals manifest; `git diff --name-only <baseline>..HEAD -- src tests scripts package.json .github` is empty, allowing sibling reports/GSD metadata.</verify>
  <done>Report contains exact automated/manual evidence and an explicit unresolved blocker/high conclusion.</done>
</task>
</tasks>

<verification>
- Playwright/axe passes or failures are reproduced and reported.
- Desktop/mobile manual evidence is concrete, not inferred from screenshots alone.
</verification>

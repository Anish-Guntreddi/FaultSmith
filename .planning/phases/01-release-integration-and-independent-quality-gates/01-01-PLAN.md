---
phase: 01-release-integration-and-independent-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - scripts/check-source-security.mjs
  - scripts/check-source-security.test.mjs
  - .planning/phases/01-release-integration-and-independent-quality-gates/01-01-BASELINE.md
autonomous: true
requirements: []
supports: [CI-01, CI-02, CI-03, CI-04, CI-05, SEC-02, SAFE-01, SAFE-02]
must_haves:
  truths:
    - Four uniquely named CI jobs exercise the existing package-script gates independently and without live OpenAI credentials.
    - Source/history scans fail safely on real secret or application-host execution evidence without printing credential contents.
    - The local aggregate quality command and validated fallback behavior remain unchanged and green.
  artifacts:
    - .github/workflows/ci.yml
    - scripts/check-source-security.mjs
    - scripts/check-source-security.test.mjs
    - 01-01-BASELINE.md containing the immutable Wave 1 implementation SHA reviewed by Plans 02–04.
  key_links:
    - package.json security:source script is invoked by Build and security CI.
    - Browser and accessibility installs Chromium before invoking the existing Playwright suite.
---

<objective>
Replace the single opaque GitHub Actions quality job with four parallel named gates and add a reproducible source/history security scan without altering learner-facing runtime behavior.
</objective>

<tasks>

<task id="01-01-01" type="auto">
  <name>Add deterministic repository source/history security scan</name>
  <files>scripts/check-source-security.mjs, scripts/check-source-security.test.mjs, package.json</files>
  <action>Implement a Node ESM security gate that scans the working tree and reachable Git objects for credential/private-key shapes by identifier only, explicitly allowlists the deliberate fake key in `src/server/fixtures.test.ts`, and scans non-test `src/` application code for host-process execution APIs and `NEXT_PUBLIC_OPENAI` exposure. Exclude generated/vendor/binary paths. Export pure matching helpers for Vitest success/failure/allowlist regressions, add `security:source`, and do not execute learner code or print matching secret contents.</action>
  <verify>`npm test -- scripts/check-source-security.test.mjs` and `npm run security:source`</verify>
  <done>Safe repository passes; controlled secret/host-execution fixtures fail in tests; output identifies only commit/file/rule.</done>
</task>

<task id="01-01-02" type="auto">
  <name>Split GitHub Actions into four parallel required-check candidates</name>
  <files>.github/workflows/ci.yml</files>
  <action>Preserve triggers, read-only permission, concurrency cancellation, Node 24, npm cache, and current action majors. Create independent jobs named exactly `Static analysis`, `Unit and integration`, `Build and security`, and `Browser and accessibility`. Use `npm ci` per job. Build/security uses full history, build, source scan, bundle scan, and moderate audit. Browser installs Chromium with dependencies and runs E2E. Add no secrets and no job dependency.</action>
  <verify>Review parsed YAML structure; run every owned package command locally: lint, typecheck, test, build, security:source, security:bundle, audit, E2E.</verify>
  <done>Four unique jobs cover CI-01–04 and call the same scripts used by local development.</done>
</task>

<task id="01-01-03" type="auto">
  <name>Prove aggregate quality and fallback invariants after gate changes</name>
  <files>None beyond prior tasks</files>
  <action>Run the full local aggregate and audit. Confirm tests still cover missing-key fixture recovery, hidden response/storage/bundle fields, arbitrary commands/files, deterministic non-promotion, and guided verified-only progress. Record exact counts/timings for later evidence but do not edit canonical docs in this plan.</action>
  <verify>`npm run quality && npm audit --audit-level=moderate && npm run security:source`</verify>
  <done>All local gates pass with no live provider call; the fallback and authority regressions remain green.</done>
</task>

<task id="01-01-04" type="auto">
  <name>Freeze the Wave 1 implementation SHA for independent review</name>
  <files>.planning/phases/01-release-integration-and-independent-quality-gates/01-01-BASELINE.md</files>
  <action>Commit the Wave 1 implementation files first, then capture that implementation commit with `git rev-parse HEAD`. Write a small baseline manifest containing the SHA, branch, UTC timestamp, clean-tree assertion, exact local gate commands/results, and the rule that Plans 02–04 must inspect this SHA in their own detached temporary worktrees. The later manifest commit is not the review target.</action>
  <verify>Manifest SHA resolves with `git cat-file -e <sha>^{commit}`; `git diff --quiet <sha>^ <sha>` is false because it contains the intended Wave 1 implementation; the implementation tree was clean when captured.</verify>
  <done>All parallel reviewers have one immutable application/config SHA independent of subsequent report commits.</done>
</task>

</tasks>

<verification>
- `git diff --check`
- `npm run quality`
- `npm audit --audit-level=moderate`
- `npm run security:source`
- `01-01-BASELINE.md` references a valid committed Wave 1 implementation SHA.
</verification>

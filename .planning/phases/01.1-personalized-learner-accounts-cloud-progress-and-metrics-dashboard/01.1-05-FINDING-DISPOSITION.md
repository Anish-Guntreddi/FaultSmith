# Phase 01.1 Plan 05 — Finding Disposition and Self-Heal Record

- **Pre-review reviewed SHA:** `5ce896b748dda638148e892e9328aa81ff7670b6`
- **Repaired candidate SHA:** `8d4e68c359a8ca13bf40acf228b9ee9f5a84bfc7` (`fix(01.1-05)`)
- **Disposition date:** 2026-07-19
- Every finding below was independently reproduced before acceptance. No schema, scanner, audit, CSP, authentication, isolation, fallback-labeling, or test authority was weakened by any repair.

## Dispositions

### SEC-01 — Assess route lacked exact-origin containment on the token-accepting surface

- **Source:** Security review (Low). **Disposition: ACCEPTED** — identity-adjacent defense-in-depth on the newest cloud-write path; qualifies under the plan's "low/medium that materially affects privacy/identity" acceptance rule.
- **Reproduction (before repair):** New adversarial test "denies cross-origin browser submissions to the token-accepting assess surface" executed at the pre-review SHA failed exactly as predicted: cross-origin assess with `Origin: https://evil.example` returned **200** where 403 was expected (`npx vitest run src/app/api/challenges/routes.test.ts` → 1 failed | 7 passed).
- **Repair:** `assertSameOrigin` promoted from a private helper in `src/app/api/progress/route.ts` into the shared server-only guard `src/server/request-guard.ts` (byte-identical logic, one implementation for both routes) and applied at the top of the assess POST handler. Origin-absent requests (non-browser clients, smokes, curl, route unit tests) pass unchanged; opaque/unparsable/mismatched origins fail closed 403 `CROSS_ORIGIN`.
- **Scope note (documented deviation):** `src/server/request-guard.ts` was not in the plan's Task 2 file list; touching it was the minimal correct home for a shared guard versus duplicating security logic across routes. No behavior of existing guard exports changed (31/31 route+guard tests pass).
- **Regression:** cross-origin denial + same-origin acceptance test permanently in `src/app/api/challenges/routes.test.ts`.
- **Affected requirements:** SEC-03, AUTH-02.
- **Rerun evidence:** route/guard suites 31/31; full unit 272/272; production smoke (origin-less) passes at the repaired SHA.

### QA-01 — Unused `passwordBoundaryRules` import; password-boundary scanner rule had no direct unit coverage

- **Source:** QA review (Low; pre-existing, logged in `deferred-items.md` for this plan). **Disposition: ACCEPTED**.
- **Reproduction (before repair):** `npm run lint` → `scripts/check-source-security.test.mjs 12:3 warning 'passwordBoundaryRules' is defined but never used` (reproduced at the pre-review SHA this session).
- **Repair:** Two tests added to `scripts/check-source-security.test.mjs`: (1) password material in server-path fixtures produces `password-boundary` findings on the exact lines without echoing the value; (2) prose that merely mentions passwords does not match. The warning is cleared by *using* the import, not by deleting it — scanner authority strengthened, not weakened.
- **Self-heal evidence during repair (scanner fails closed):** the first fixture draft used a secret-length literal and `npm run security:source` correctly **failed** on it (`working-tree:scripts/check-source-security.test.mjs [secret-assignment]`). The fixture was shortened below every secret-rule threshold so it trips only the rule under test. This accidental live-fire proof that the secret scanner blocks secret-shaped strings even in test files is recorded here as positive evidence; no allowlist was added.
- **Regression:** the two tests themselves. **Affected requirements:** AUTH-03, PRIV-01, QA-03.
- **Rerun evidence:** `npm run lint` exits with **zero** warnings; scanner suite 15/15; `npm run security:source` passes (483 files, 58 reachable commits).

### PROD-01 / QA-02 — "Continue as guest" control was inert and unannounced

- **Source:** Product review (Low) + QA review (Low), same root cause. **Disposition: ACCEPTED** — materially affects accessibility and the guest-first product message per the plan's acceptance rule.
- **Reproduction (before repair):** source inspection at the pre-review SHA: `onClick={() => setMode("summary")}` while the control renders only in `summary` mode — provably no state change, no live-region output.
- **Repair:** `continueAsGuest()` added to the sync hook: clears any collision notice and announces "Guest practice is active. Progress stays on this device unless you add an optional account." through the existing `role="status"` live region; the button also resets password form state. No storage, network, or identity behavior changes — guest remains the default, not an opt-in.
- **Regression:** new Firebase-mode browser test "continuing as guest announces confirmation and keeps device storage" (suite grew 15 → 16 scenarios).
- **Affected requirements:** AUTH-01, PERS-01.
- **Rerun evidence:** `npm run test:e2e:firebase` 16/16 at the repaired working tree.

### SEC-OBS-A — Tolerant cloud-document parsing degrades to the empty profile

- **Disposition: REJECTED (no change), consciously accepted behavior.** Firestore is writable only by the server (deny-all rules + Admin-only mediation, both emulator-proven), so an unparseable document implies operator-level intervention; degrading to empty availability-preserving state never fabricates progress and never blocks learning. Changing this would trade learner availability for no adversarial gain.

### SEC-OBS-B — Multi-instance rate limiting / App Check remain deployment-gated

- **Disposition: REJECTED as a Plan 05 repair; remains an explicit pending external gate.** In-memory per-instance and per-scope caps are the correct credential-free maximum; Netlify edge controls and App Check observation belong to the human-gated deployment phase and are labeled `[pending-external]` in every report. No documentation claims them as done.

## Gate reruns after repairs

| Gate | Result | State |
|---|---|---|
| `npm run security:source` | pass — 483 files, 58 reachable commits | repaired tree |
| `npm run quality` (lint 0 warnings, typecheck, unit **272/272**, `test:firebase` 23/23, build, bundle 21 artifacts, default e2e 13 passed/16 env-gated skips) | pass | repaired tree (all runtime changes present) |
| `npm audit --audit-level=moderate` | 0 vulnerabilities | repaired tree |
| `npm run test:e2e:firebase` | **16/16** | repaired tree |
| `npm run smoke:fallback` | pass — `mode=fallback stages=7 sha=8d4e68c359a8` | committed candidate |
| `npm run smoke:production` | pass — headers verified, cache no-store, `sha=8d4e68c359a8` | committed candidate |

Sequencing note for honesty: the quality chain executed with every runtime repair in the tree; the only edit after that run was the scanner-test fixture shortening (a `scripts/*.test.mjs`-only change), re-verified by lint (clean), the 15/15 scanner suite, and a passing `security:source`. The complete single-SHA gate proof is re-executed once more against the final post-documentation candidate in Task 3.

## Targeted reviewer rechecks against `8d4e68c359a8`

- **Security recheck (SEC-01 scope only):** cross-origin assess now fails 403 `CROSS_ORIGIN` with no detail leakage; origin-less production smoke unchanged; no other guard behavior moved. Recheck: **satisfied**.
- **QA recheck (QA-01/QA-02 scope only):** lint gate fully clean; password-boundary rule now has direct authority coverage with no-leak assertions; guest activation is announced via the live region and covered by an automated browser scenario. Recheck: **satisfied**.
- **Product recheck (PROD-01 scope only):** all three access paths now respond meaningfully to activation; guest remains default with no behavior gate added. Recheck: **satisfied**.

Targeted rechecks clear the specific findings only; per the plan, final certification of the release candidate requires all three full reviews against the exact post-documentation SHA (Task 3).

## Outcome

- Accepted: 3 (SEC-01, QA-01, PROD-01/QA-02 — all Low; every accepted finding repaired with a permanent regression).
- Rejected: 2 informational observations with recorded rationale.
- **No accepted blocker or high exists.** Cloud-off and emulator modes are green on the repaired candidate.

---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 06
type: checkpoint
wave: 5
depends_on: [01.1-05]
files_modified:
  - .planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md
  - .planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-NETLIFY-PREVIEW-EVIDENCE.md
  - docs/BUILD_LOG.md
  - docs/ROADMAP.md
  - docs/THREAT_MODEL.md
  - docs/TESTING.md
  - docs/DEPLOYMENT.md
  - docs/SUBMISSION.md
  - docs/COMPLETION_REPORT.md
autonomous: false
requirements: [AUTH-01, AUTH-02, CLOUD-01, CLOUD-02, PRIV-01, SEC-03, QA-03, DEP-05]
must_haves:
  truths:
    - Real Firebase proof occurs only after the user privately creates/configures a Spark project and confirms no credential value to the agent.
    - Netlify preview occurs only after a separate explicit deployment approval and promotes only the exact reviewed SHA with host rate/secret controls.
    - Any failed identity/isolation/CSP/log/quota/rollback gate disables cloud mode; no deadline pressure weakens security or the local fallback.
  artifacts:
    - Sanitized SHA-bound real Firebase evidence, optionally approved Netlify preview evidence, and honest canonical status updates.
  key_links:
    - Real Firebase proof validates emulator assumptions; Netlify preview validates host behavior. Neither substitutes for the other or for OpenAI live proof.
    - Credentials are configured/removed privately by the user and never accepted as CLI arguments, chat text, evidence, or repository content.
---

<objective>
At explicit human checkpoints, prove the reviewed candidate with a real Firebase Spark project and then—only after separate approval—on a Netlify Deploy Preview; otherwise ship the cloud-off local dashboard.
</objective>

<tasks>
<task id="01.1-06-01" type="checkpoint:human-action" gate="blocking">
  <name>User privately creates and configures the Firebase Spark project</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md</files>
  <action>Stop for the user to create a Firebase project on Spark without enabling billing, register one web app, enable Google Authentication, create the one free Firestore database, deploy default-deny rules, authorize only localhost and the intended Netlify preview/production domains, create/restrict server service-account access where practicable, and place public/server values privately in ignored local environment configuration. The agent must not request, inspect, echo, edit, delete, or receive any credential. The user confirms only safe booleans: cloud health ready, Spark/no-billing, Google provider enabled, Firestore/rules ready.</action>
  <verify>Health returns only the reviewed coarse cloud state; source scan is not run while real credentials remain in a scanned ignored file.</verify>
  <done>User reports safe readiness booleans without disclosing credentials or identity data.</done>
</task>

<task id="01.1-06-02" type="checkpoint:human-verify" gate="blocking">
  <name>Run one controlled real Firebase local smoke and configuration-off rollback</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md, docs/BUILD_LOG.md, docs/ROADMAP.md, docs/THREAT_MODEL.md, docs/TESTING.md, docs/COMPLETION_REPORT.md</files>
  <action>Start the exact reviewed production build. In a dedicated test identity, verify public guest Lesson 1, Google sign-in/cancellation, one verified fixture completion, strict dashboard metrics, cloud persistence, clean-browser cross-session restoration, sign-out guest continuity, a second test user's isolation, cloud-data deletion, bounded logs, no client Admin/secret data, Firestore usage, and local behavior during simulated cloud unavailability. Record only SHA, UTC time, safe mode/status facts, counts, stage pass/fail, and digests—no UID/email/config/token/document path/content. Then the user stops the server and privately removes real local Firebase credentials before source/history scanning. Rebuild with cloud disabled and prove local dashboard, fixture fallback, production smoke, and security scans. If any real-cloud or rollback gate fails, set cloud feature off and document cloud sync as not shipped.</action>
  <verify>Credentialed smoke checklist passes; after private cleanup, `npm run security:source && npm run quality && npm run test:firebase && npm audit --audit-level=moderate` plus fallback/production smoke pass.</verify>
  <done>Real Firebase and configuration-off behavior are objectively proven on the runtime SHA, or cloud mode is explicitly disabled.</done>
</task>

<task id="01.1-06-03" type="checkpoint:decision" gate="blocking">
  <name>Obtain separate Netlify Deploy Preview approval and verify the host</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-NETLIFY-PREVIEW-EVIDENCE.md, docs/BUILD_LOG.md, docs/ROADMAP.md, docs/THREAT_MODEL.md, docs/TESTING.md, docs/DEPLOYMENT.md, docs/SUBMISSION.md, docs/COMPLETION_REPORT.md</files>
  <action>Request explicit approval for the exact Netlify site/account/region/branch/SHA and paid OpenAI/Firebase secret exposure. If approved, connect the GitHub repository, use the reviewed Node/OpenNext build settings, configure public Firebase values per deploy context and server-only Firebase/OpenAI secrets in Netlify, enable skew protection if applicable, add verified path-level rate rules for progress and stricter AI routes, set function/concurrency/usage/spend alerts, and inspect deploy logs to confirm rules. Run production smoke against the Deploy Preview for unauthenticated guest access, health, security headers/CSP, no-store, local dashboard, Google sign-in and clean-session sync, cross-user isolation, fixture fallback, optional OpenAI live only if separately proven/authorized, safe logs, 1440x900/390x844, and configuration-off rollback. Do not promote production until the user approves the preview evidence. If approval is absent or any gate fails, keep deployment pending and preserve the green local candidate.</action>
  <verify>Sanitized preview evidence binds URL origin, SHA, UTC time, public mode labels, rate-control confirmation, stage pass/fail, counts, and digests only; unauthenticated external smoke and all local gates remain green.</verify>
  <done>DEP-05 has real preview evidence and explicit promotion decision, or remains honestly pending without weakening the local submission candidate.</done>
</task>
</tasks>

<verification>
- No credential appears in chat, command arguments, Git, logs, evidence, or screenshots.
- Real Firebase and Netlify preview evidence bind to the exact runtime SHA and remain distinct from emulator/OpenAI proof.
- Failure path is explicit: disable cloud configuration, rerun local/fallback gates, and proceed with the personalized local dashboard.
</verification>

---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 06
type: checkpoint
wave: 5
depends_on: [01.1-05]
files_modified:
  - .planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md
  - .planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-NETLIFY-PREVIEW-EVIDENCE.md
  - docs/BUILD_LOG.md
  - docs/ROADMAP.md
  - docs/THREAT_MODEL.md
  - docs/TESTING.md
  - docs/DEPLOYMENT.md
  - docs/SUBMISSION.md
  - docs/COMPLETION_REPORT.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, CLOUD-01, CLOUD-02, PRIV-01, SEC-03, QA-03, DEP-05]
must_haves:
  truths:
    - Real Firebase proof occurs only after the user privately creates/configures a Spark project and confirms no credential value to the agent.
    - Netlify preview occurs only after a separate explicit deployment approval and promotes only the exact reviewed SHA with host rate/secret controls.
    - Any failed identity/isolation/CSP/log/quota/rollback gate disables cloud mode; no deadline pressure weakens security or the local fallback.
  artifacts:
    - Sanitized SHA-bound real Firebase evidence, optionally approved Netlify preview evidence, and honest canonical status updates.
  key_links:
    - Real Firebase proof validates emulator assumptions; Netlify preview validates host behavior. Neither substitutes for the other or for OpenAI live proof.
    - Credentials are configured/removed privately by the user and never accepted as CLI arguments, chat text, evidence, or repository content.
---

<objective>
At explicit human checkpoints, prove the reviewed candidate with a real Firebase Spark project and then—only after separate approval—on a Netlify Deploy Preview; otherwise ship the cloud-off local dashboard.
</objective>

<tasks>
<task id="01.1-06-01" type="checkpoint:decision" gate="blocking">
  <name>Choose private Firebase configuration or the approved cloud-off release path</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md</files>
  <action>Ask the user to choose either **configure real Firebase now** or **ship the reviewed cloud-off local dashboard**. If configuring, the user privately creates a Spark project without billing, registers one web app, enables email/password and Google Authentication with one-account-per-email behavior, sets a required password policy, enables email-enumeration protection, customizes verification/reset templates without learner secrets, allows only reviewed localhost and intended Netlify preview/production domains/action continue URLs, creates the one free Firestore database, deploys default-deny rules, creates/restricts server service-account access where practicable, and places public/server values privately in ignored local environment configuration. The agent must not request, inspect, echo, edit, delete, or receive any credential or password. The user confirms only safe booleans. If the user declines, lacks credentials, or any setup boolean is false, record real Firebase, provider linking, and DEP-05 as pending/not shipped; keep cloud configuration disabled; run the Task 06-02 cloud-off rollback gates; and complete Phase 01.1 on the approved local personalized-dashboard candidate rather than blocking.</action>
  <verify>Health returns only the reviewed coarse cloud state; source scan is not run while real credentials remain in a scanned ignored file.</verify>
  <done>The user either reports safe readiness booleans without disclosing credentials/identity data or explicitly selects the documented cloud-off release path.</done>
</task>

<task id="01.1-06-02" type="checkpoint:human-verify" gate="blocking">
  <name>Run one controlled real Firebase local smoke and configuration-off rollback</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-REAL-FIREBASE-EVIDENCE.md, docs/BUILD_LOG.md, docs/ROADMAP.md, docs/THREAT_MODEL.md, docs/TESTING.md, docs/COMPLETION_REPORT.md</files>
  <action>If Task 06-01 selected real configuration, start the exact reviewed production build. With dedicated non-personal test identities, verify public guest Lesson 1; email/password creation and policy guidance; unverified cloud denial with local continuity; verification/resend cooldown; verified login and generic reset; Google sign-in/cancellation; provider-collision behavior and UID-preserving link only if the test-gated feature is enabled; one verified fixture completion; strict shared dashboard metrics; cloud persistence; clean-browser cross-session restoration; sign-out guest continuity; a second test user's isolation; cloud/account-data deletion with recent authentication; bounded logs; no password/client Admin/secret data; Firestore usage; and local behavior during simulated cloud unavailability. Record only SHA, UTC time, safe mode/status facts, counts, stage pass/fail, and digests—no UID/email/password/config/token/document path/content. Whether real configuration was selected, declined, or failed, end by privately removing/omitting real local Firebase credentials, rebuilding with cloud disabled, and proving the local dashboard, fixture fallback, production smoke, and security scans. If any real-cloud gate fails, document cloud sync as not shipped; if any cloud-off rollback gate fails, return to implementation/self-heal rather than weakening it.</action>
  <verify>If real Firebase was configured, its credentialed smoke checklist passes; in every branch, after private cleanup/omission, `npm run security:source && npm run quality && npm run test:firebase && npm audit --audit-level=moderate` plus cloud-off fallback/production smoke pass.</verify>
  <done>Configuration-off behavior is objectively proven on the runtime SHA in every branch; real Firebase is additionally proven only when configured, otherwise it remains honestly pending and cloud mode is disabled.</done>
</task>

<task id="01.1-06-03" type="checkpoint:decision" gate="blocking">
  <name>Obtain separate Netlify Deploy Preview approval and verify the host</name>
  <files>.planning/phases/01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard/01.1-06-NETLIFY-PREVIEW-EVIDENCE.md, docs/BUILD_LOG.md, docs/ROADMAP.md, docs/THREAT_MODEL.md, docs/TESTING.md, docs/DEPLOYMENT.md, docs/SUBMISSION.md, docs/COMPLETION_REPORT.md</files>
  <action>Only if real Firebase passed Task 06-02, request explicit approval for the exact Netlify site/account/region/branch/SHA and Firebase service-credential exposure. If approved, connect the GitHub repository, use the reviewed Node/OpenNext build settings, configure public Firebase values per deploy context and the server-only Firebase secret in Netlify, enable skew protection if applicable, add verified path-level rate rules for progress/auth-action surfaces and preserve stricter AI-route controls, set function/concurrency/usage alerts, and inspect deploy logs to confirm rules. Do not configure or expose `OPENAI_API_KEY` in this phase; an existing separately approved and proven Phase 2 configuration may be observed but is not a prerequisite for DEP-05. Run production smoke against the Deploy Preview for unauthenticated guest access, health, security headers/CSP, no-store, local dashboard, verified email/password and Google sign-in with clean-session sync, verification/reset email actions, provider-collision safe behavior, cross-user isolation, fixture fallback, safe logs, 1440x900/390x844, and configuration-off rollback. Inject or simulate a stalled/degraded progress request at the preview boundary, measure that it terminates within the documented cloud timeout, prove the current report/local progress remain available, inspect sanitized response/log evidence, and retry once to prove no duplicate attempt/import write. Do not promote production until the user approves the preview evidence. If approval is declined/absent or any gate fails, record DEP-05 pending, keep cloud/deployment status honest, rerun the cloud-off gates, and complete or retain Phase 01.1 on the green local candidate.</action>
  <verify>Sanitized preview evidence binds URL origin, SHA, UTC time, public mode labels, measured degraded-request timeout, no-duplicate retry fact, rate-control confirmation, stage pass/fail, counts, and digests only; unauthenticated external smoke and all local gates remain green.</verify>
  <done>DEP-05 has real preview evidence and explicit promotion decision, or remains honestly pending without weakening the local submission candidate.</done>
</task>
</tasks>

<verification>
- No credential appears in chat, command arguments, Git, logs, evidence, or screenshots.
- Real Firebase and Netlify preview evidence bind to the exact runtime SHA and remain distinct from emulator/OpenAI proof.
- Failure path is explicit: disable cloud configuration, rerun local/fallback gates, and proceed with the personalized local dashboard.
</verification>

---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - .env.example
  - firebase.json
  - firestore.rules
  - vitest.firebase.config.mts
  - .github/workflows/ci.yml
  - src/client/firebase-auth.ts
  - src/client/firebase-auth.test.ts
  - tests/firebase/firestore.rules.test.ts
  - scripts/check-source-security.mjs
  - scripts/check-source-security.test.mjs
  - scripts/check-client-bundle.mjs
  - scripts/check-client-bundle.test.mjs
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, PRIV-01, SEC-03, QA-03]
must_haves:
  truths:
    - Adding current Firebase libraries cannot make an unconfigured build, local startup, CI, or guest workflow depend on Firebase.
    - Client configuration exposes only documented Firebase web values; service credentials and Admin modules remain server-only and scan-protected.
    - Emulator tests use a demo project and direct browser Firestore access is denied even for an authenticated UID.
  artifacts:
    - Reviewed Node/dependency/runtime contract, lazy client auth adapter, emulator/rules configuration, and CI command.
    - Firebase-specific source/history/client-bundle regression coverage before any real credential exists.
  key_links:
    - Current Firebase Admin Node support and Netlify runtime use Node 22 or newer; CI remains Node 24.
    - Normal app configuration defaults cloud progress off and imports no privileged module into the client graph.
---

<objective>
Install the credential-free Firebase/runtime/emulator/security foundation and a lazy optional email/password + Google auth client without altering the local learning path.
</objective>

<tasks>
<task id="01.1-02-01" type="auto">
  <name>Align runtime and install reviewed Firebase dependencies without weakening gates</name>
  <files>package.json, package-lock.json, .nvmrc, .env.example, .github/workflows/ci.yml</files>
  <action>Read the current package release metadata and bundled Next.js 16 docs before installation. Add the modular Firebase web SDK, Firebase Admin SDK, pinned Firebase CLI/rules testing development support, and no chart/auth wrapper library. Align `engines.node` and `.nvmrc` to a reviewed Node 22-or-newer runtime compatible with current Firebase Admin while retaining CI Node 24. Document empty public Firebase web variables, one cloud feature flag, Firebase project ID, and one server-only service-account secret placeholder; never add a value. Add non-watch `test:firebase` and include it in the complete quality path only after a demo-project emulator is configured. In CI, explicitly set up a Java LTS runtime and run emulator tests inside the existing Unit and integration gate so branch-protection names remain stable. Run `npm audit --audit-level=moderate`; do not accept a vulnerable or audit-skipping dependency state.</action>
  <verify>`npm ci`; `npm run lint`; `npm run typecheck`; `npm test`; `npm audit --audit-level=moderate`.</verify>
  <done>Runtime/dependency configuration is reproducible, audit-clean, and remains credential-free.</done>
</task>

<task id="01.1-02-02" type="auto">
  <name>Add lazy Firebase Auth client, default-deny rules, emulator tests, and leakage gates</name>
  <files>firebase.json, firestore.rules, vitest.firebase.config.mts, src/client/firebase-auth.ts, src/client/firebase-auth.test.ts, tests/firebase/firestore.rules.test.ts, scripts/check-source-security.mjs, scripts/check-source-security.test.mjs, scripts/check-client-bundle.mjs, scripts/check-client-bundle.test.mjs</files>
  <action>Implement a browser-only adapter that validates the complete public configuration, initializes Firebase lazily only after the learner requests sync, and exposes auth subscription, email/password create/login, `validatePassword`, verification email/resend, password reset, Google popup, explicit provider-link attempt, sign-out, and fresh-ID-token operations. Collapse provider errors into bounded enumeration-resistant states without returning password/token/email/provider payloads; verification/reset have local cooldown state and no server password path. Missing/partial/off configuration returns `local_only` without throwing at import/build. Configure Authentication and Firestore emulators under a `demo-faultsmith` project; seed/test guest, unverified email, verified email, Google, collision, UID-preserving link-or-safe-fallback, and sign-out states. Deploy Firestore rules that deny all browser reads/writes because the server owns persistence; test unauthenticated and authenticated direct-client denial. Extend source/history scanning to catch password fields crossing server/persistence/evidence boundaries, Firebase service-account JSON/private keys/bearer tokens/publicized server secrets, and client-bundle scanning to reject Admin SDK/credential markers, while explicitly treating expected public Firebase web configuration names as public metadata rather than authorization. Do not allow real-looking values in fixtures.</action>
  <verify>`npx vitest run src/client/firebase-auth.test.ts scripts/check-source-security.test.mjs scripts/check-client-bundle.test.mjs`; `npm run test:firebase`; `npm run security:source`; `npm run build`; `npm run security:bundle`.</verify>
  <done>Optional Auth and emulator infrastructure are usable without credentials, direct Firestore is denied, and secret/server boundaries fail closed.</done>
</task>
</tasks>

<verification>
- Build and run with every Firebase variable absent and partially set; health and guest learning remain ready.
- Emulator/rules tests cannot contact a real Firebase project.
- Source and bundle scanners catch private/admin material without flagging expected empty/public config names.
- Auth adapter tests prove unverified, reset, enumeration-resistant, provider-collision, UID continuity, and password non-persistence behavior without contacting real Firebase.
</verification>

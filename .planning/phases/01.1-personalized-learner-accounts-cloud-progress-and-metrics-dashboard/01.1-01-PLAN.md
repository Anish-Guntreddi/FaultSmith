---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/learning-paths.ts
  - src/lib/learning-paths.test.ts
  - src/lib/progress-contracts.ts
  - src/lib/progress-contracts.test.ts
  - src/lib/progress-metrics.ts
  - src/lib/progress-metrics.test.ts
  - src/lib/progress-merge.ts
  - src/lib/progress-merge.test.ts
  - src/components/progress-dashboard.tsx
  - src/components/faultsmith-app.tsx
  - tests/e2e/guided-learning.spec.ts
autonomous: true
requirements: [PERS-01, PERS-02, CLOUD-02, QA-03]
must_haves:
  truths:
    - A guest can understand personal progress from validated browser-local evidence without Firebase, an account, or an OpenAI request.
    - Metrics and recommendations are deterministic, bounded, explainable, and never turn test iteration into a penalty or claim certification.
    - Existing v1 learning progress restores safely; invalid/tampered state and private learner content remain excluded.
  artifacts:
    - Strict client-safe progress/attempt contracts with migration, merge, and metric selectors.
    - Accessible My Progress dashboard integrated into the existing configure experience.
  key_links:
    - The dashboard derives from the same verified assessment fields and lesson registry already used by the roadmap.
    - Cloud-ready contracts contain no source, hypothesis, explanation, hint text, test output, provider data, identity token, or credential field.
---

<objective>
Build the complete credential-free personalized-learning domain and local My Progress experience before introducing any Firebase dependency.
</objective>

<tasks>
<task id="01.1-01-01" type="auto">
  <name>Define strict progress, attempt, migration, merge, and metric contracts</name>
  <files>src/lib/learning-paths.ts, src/lib/learning-paths.test.ts, src/lib/progress-contracts.ts, src/lib/progress-contracts.test.ts, src/lib/progress-metrics.ts, src/lib/progress-metrics.test.ts, src/lib/progress-merge.ts, src/lib/progress-merge.test.ts</files>
  <action>Create client-safe Zod contracts for a versioned local/cloud learning profile and a capped attempt summary containing only approved lesson/project/skill/difficulty/source/status, bounded score dimensions, hints, test runs, changed lines, a duration bucket, completion time, idempotency identifier, and `server_verified | local_import` provenance. Preserve v1 learning-progress parsing and migrate it deterministically without fabricating history. Add helpers for monotonic per-lesson merge, duplicate/idempotency replacement, 50-attempt retention, phase completion, verified score averages, independent-solve rate, strongest practiced skill, reinforcement priority, and the existing next-lesson/catalog recommendation. Test unknown keys, unknown lessons/skills, duplicate/out-of-order records, over-range/oversized values, malicious learner-content fields, empty state, unverified-only state, and the rule that test count is descriptive rather than a score penalty.</action>
  <verify>`npx vitest run src/lib/learning-paths.test.ts src/lib/progress-contracts.test.ts src/lib/progress-metrics.test.ts src/lib/progress-merge.test.ts`; `npm run lint`; `npm run typecheck`.</verify>
  <done>All personalized metrics and synchronization DTOs are strict, deterministic, privacy-minimal, and backwards compatible with existing local progress.</done>
</task>

<task id="01.1-01-02" type="auto">
  <name>Add bounded local attempt history and the guest My Progress dashboard</name>
  <files>src/components/progress-dashboard.tsx, src/components/faultsmith-app.tsx, tests/e2e/guided-learning.spec.ts</files>
  <action>Add a dedicated local attempt-summary storage key separate from the source/prose-rich active attempt and anonymous telemetry. On every completed assessment, derive and persist only the strict bounded summary; record roadmap completion only when deterministic assessment status is verified. Render My Progress from the strict profile/history selectors with storage label `On this device`, 0/9 and phase progress, score dimensions, hints/independent solve evidence, test-run process evidence, strongest/reinforcement skill, recent attempts, and reasoned next recommendation. Use compact evidence cards/bars within the established visual system; include zero/first/partial/complete states and explicit practice-not-certification language. Preserve Guided Roadmap as the default and keep the first lesson launch reachable. Extend Playwright/axe coverage for existing v1 migration, verified update, failing non-completion, refresh restoration, keyboard navigation, 1440x900, and 390x844 without overflow.</action>
  <verify>`npx playwright test tests/e2e/guided-learning.spec.ts`; `npm run lint`; `npm run typecheck`; `npm test`; `npm run build`; `npm run security:bundle`.</verify>
  <done>The app demonstrates useful individualized progression entirely offline while every existing beginner and fallback workflow remains intact.</done>
</task>
</tasks>

<verification>
- Complete the primary guided fixture and prove the dashboard changes only after verified submission.
- Restore a v1 profile and reject a tampered profile containing prose or unknown IDs.
- Run focused domain and browser checks plus production bundle scan.
</verification>

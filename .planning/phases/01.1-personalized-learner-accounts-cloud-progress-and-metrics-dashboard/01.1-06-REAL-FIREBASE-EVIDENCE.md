# Plan 01.1-06 — Real Firebase Evidence (Task 06-02, automated stages)

**Status:** Automated real-project smoke PASSED (22/22 stages). Browser/Google
stages and the cloud-off rollback gates remain pending at the human checkpoint
below. Nothing in this file is emulator evidence and nothing here substitutes
for OpenAI live proof or Netlify preview proof.

## Binding

- Runtime SHA: `9c6c861d28b2` (working tree identical to the triple-approved
  reviewed runtime SHA `ae39503` — every later commit is `.planning/`
  documentation only; verified via `git diff --name-only ae39503..HEAD`).
- Automated smoke completed (UTC): 2026-07-19T19:19:26Z (run window
  ~19:07–19:19 UTC, includes deliberate rate-window spacing).
- Server: exact reviewed production build (`next build` + `next start`),
  cloud-configured via ignored local environment only.
- Results digest (SHA-256 of the sanitized machine-readable stage summary):
  `c2a210f6676ed93a6512184ca5b0140c39cb10fa0f560d91332cb1de6eb9060f`

## Sanitization statement

No credential, password, email address, UID, token, project configuration
value, document path, or document content was printed, logged, committed, or
recorded — here or anywhere. Evidence is limited to stage names, booleans,
HTTP statuses, sanitized provider error-code words, counts, and digests.
Configuration presence was verified as booleans only (all seven expected
ignored-environment names present; cloud flag true; public/server project
identifiers match; server credential shape structurally valid; provider-linking
flag absent so linking correctly remains default-unsupported; no OpenAI key; no
emulator hosts).

## Setup confirmation (user-reported safe booleans, 2026-07-19)

Auth initialized: yes. Email/password provider: yes. Password policy ENFORCE
min 12 / max 128: yes. Email-enumeration protection: yes. Google provider with
support email: yes. Authorized domains restricted to the reviewed localhost
plus the two default project auth domains: yes. Public web config privately
placed in ignored local env: yes. Server service account privately placed in
ignored local env, temp file deleted: yes. Repository default-deny
`firestore.rules` deployed: yes. Email templates: safe defaults retained (no
learner secrets), customization deferred until a stable public domain exists.

## Configuration and surface facts (booleans)

- `/api/health` returns only the reviewed coarse facts
  (`status`, `liveOpenAIConfigured:false`, `fixtureFallback:"ready"`); no
  cloud, project, or identity detail is exposed. No source scan was run while
  real credentials remain in the scanned ignored file.
- Cloud-configured header surface matches the reviewed shape exactly (17/17
  boolean checks): CSP widens only by the empirically approved origins
  (identitytoolkit + securetoken in `connect-src`, exactly one validated
  auth-domain origin in `frame-src`, `apis.google.com` in `script-src`), no
  wildcards, no `unsafe-eval`; COOP is `same-origin-allow-popups` (cloud-on
  only); CORP/`X-Frame-Options`/nosniff/referrer/permissions policies and
  `X-Powered-By` absence unchanged from baseline.
- Public guest lifecycle: `smoke:fallback` passed — mode=fallback, 7 stages,
  runtime SHA `9c6c861d28b2` (guest Lesson 1 and the full prevalidated
  fixture lifecycle require no account).

## Automated real-project stages (22/22 PASS)

| # | Stage | Result | Sanitized facts |
|---|-------|--------|-----------------|
| 1 | password-policy-guidance | PASS | weak sign-up rejected, code `PASSWORD_DOES_NOT_MEET_REQUIREMENTS` |
| 2 | email-password-creation | PASS | dedicated non-personal test identity, compliant password |
| 3 | duplicate-email-rejected | PASS | one account per email, code `EMAIL_EXISTS` |
| 4 | unverified-progress-denied | PASS | HTTP 401 `UNAUTHORIZED` |
| 5 | unverified-local-continuity | PASS | full deterministic lifecycle passed; assess `cloudSync=unauthorized` twice; 0 cloud documents written |
| 6 | verification-send | PASS | first send 200; immediate resend 400 `TOO_MANY_ATTEMPTS_TRY_LATER` (cooldown) |
| 7 | test-identity-verified-via-admin | PASS | controlled-test verification of the dedicated identity via the private server credential (no inbox exists for the test domain; the real emailed-link click remains a human browser step) |
| 8 | verified-login | PASS | fresh sign-in token accepted by server |
| 9 | enumeration-resistant-signin | PASS | wrong-password and unknown-user share one generic code `INVALID_LOGIN_CREDENTIALS` |
| 10 | generic-password-reset | PASS | known and unknown addresses return identical generic success (200/200) |
| 11 | verified-lifecycle-cloud-saved | PASS | assess `cloudSync=cloud_saved` twice; snapshot: 2 attempts (1 `server_verified`+`verified`), 1 completion, import marker false; exactly 1 profile document |
| 12 | idempotent-replay-collapse | PASS | identical replayed lifecycle: attempts 2 → 2 (SHA-256 idempotency collapse) |
| 13 | one-time-import | PASS | first import 200 with marker true; replay 409 `IMPORT_ALREADY_COMPLETED` |
| 14 | cross-session-restore | PASS | fresh sign-in session read identical data (2 attempts, 1 completion, marker true) |
| 15 | cross-user-isolation | PASS | second verified user sees empty profile (0 attempts, 0 completions, marker false) |
| 16 | direct-firestore-denied | PASS | direct client Firestore REST reads denied 403 for anonymous, owner, and other-user identity states (default-deny rules live) |
| 17 | bounded-firestore-usage | PASS | exactly 1 `learningProfiles` document at peak |
| 18 | cloud-unavailable-local-fallback | PASS | degraded instance (structurally invalid server credential): health 200, progress 503 `CLOUD_UNAVAILABLE` retryable, assess `cloudSync=cloud_unavailable` twice, guest lifecycle fully green |
| 19 | cloud-data-deletion | PASS | recent-auth DELETE 200 `deleted:true`; snapshot empty; 0 documents remain |
| 20 | account-deletion-recent-auth | PASS | both test accounts self-deleted with recent tokens (200/200) |
| 21 | no-test-users-remain | PASS | project user count after cleanup: 0 |
| 22 | bounded-logs-no-secrets | PASS | 52 build/server log lines scanned; no key/token/password/email/UID/private-key material found |

## Honest scope notes

- Stage 7 verified the dedicated test identity via the private server Admin
  credential because the non-personal test addresses have no inbox. This
  proves the server's provider-independent `email_verified` enforcement
  against the real project; the human checkpoint still exercises the real
  emailed verification action link in a browser.
- Provider linking remains default-unsupported (capability flag absent), so
  UID-preserving link behavior is correctly out of scope per plan; provider
  collision handling requires a real Google account and remains pending below.
- Test-run counts are process evidence only; no metric authority was changed.

## Pending at the human checkpoint (not yet proven)

1. Google sign-in success, cancellation, and provider-collision guidance in a
   real browser with a real Google account (server left running at
   `http://127.0.0.1:3122`).
2. Real verification/reset email action links from a real inbox.
3. Clean-browser cross-session UI restoration, sign-out guest continuity, and
   dashboard rendering at 1440×900 / 390×844 against the real project.
4. Cloud-off rollback gates: blocked until the user privately removes/omits
   the real local credentials (the agent never inspects, edits, or deletes
   them); then rebuild cloud-off and run
   `npm run security:source && npm run quality && npm run test:firebase &&
   npm audit --audit-level=moderate` plus fallback/production smoke.
5. Netlify Deploy Preview (Task 06-03) is separately approval-gated and has
   NOT been approved; DEP-05 remains pending.

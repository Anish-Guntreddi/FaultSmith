---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 04
type: execute
wave: 3
depends_on: [01.1-03]
files_modified:
  - src/components/progress-sync.tsx
  - src/components/progress-dashboard.tsx
  - src/components/faultsmith-app.tsx
  - src/client/firebase-auth.ts
  - src/lib/progress-merge.ts
  - next.config.ts
  - playwright.firebase.config.ts
  - tests/e2e/guided-learning.spec.ts
  - tests/e2e/cloud-progress.spec.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [PERS-01, PERS-02, AUTH-01, CLOUD-02, PRIV-01, SEC-03, QA-03]
must_haves:
  truths:
    - Guest use remains the visible default; sign-in is optional, understandable, cancellable, and never creates a navigation/login wall.
    - Local progress renders immediately, synchronized data merges without loss/false provenance, and degraded cloud state remains visibly saved on-device.
    - Auth/CSP changes allow only empirically required exact origins and preserve keyboard, axe, responsive, refresh, reset, and fallback behavior.
  artifacts:
    - Accessible sync controls/status, cloud-aware dashboard, import/delete/sign-out flows, exact CSP, and signed/degraded browser coverage.
  key_links:
    - The client asks Firebase for a fresh token only at protected request time and never stores it in FaultSmith localStorage.
    - Cloud UI consumes only strict DTOs and local merges; raw Firebase user/token/provider objects do not enter app persistence or report state.
---

<objective>
Integrate optional Google synchronization into the learner experience and prove that identity, network, CSP, and Firebase failures cannot disrupt guest learning.
</objective>

<tasks>
<task id="01.1-04-01" type="auto">
  <name>Add optional sign-in, synchronization, merge, import, sign-out, and deletion UX</name>
  <files>src/components/progress-sync.tsx, src/components/progress-dashboard.tsx, src/components/faultsmith-app.tsx, src/client/firebase-auth.ts, src/lib/progress-merge.ts</files>
  <action>Add a compact non-blocking `Sync progress with Google` control to My Progress, never before the learner can access the roadmap. Model explicit `On this device`, `Signing in`, `Syncing`, `Synced with Google`, and `Saved on this device—cloud unavailable` states with accessible live text and retry. After intentional sign-in, obtain a fresh ID token for same-origin progress calls, fetch cloud DTO, perform strict monotonic merge, and offer/import bounded existing local progress with `local_import` provenance exactly once. On subsequent assessment, attach a fresh token but update local report/progress first; merge the returned/fetched server record without duplication. Sign-out returns to guest data; cloud-data deletion requires explicit confirmation and reports real failure; account/provider deletion, if included, requires recent authentication and must follow data deletion. Never store Firebase user objects, UID, email, display name, token, provider response, or project config in FaultSmith localStorage/events.</action>
  <verify>`npx vitest run src/client/firebase-auth.test.ts src/lib/progress-merge.test.ts`; `npm run lint`; `npm run typecheck`; `npm test`.</verify>
  <done>Optional sync is clear and useful while local progress stays first-class and resilient.</done>
</task>

<task id="01.1-04-02" type="auto">
  <name>Harden CSP and browser/accessibility behavior across auth and degraded states</name>
  <files>next.config.ts, playwright.firebase.config.ts, tests/e2e/guided-learning.spec.ts, tests/e2e/cloud-progress.spec.ts, package.json, package-lock.json</files>
  <action>Read the bundled Next.js 16 CSP guide before changes. Add only the exact Firebase Identity Toolkit/Secure Token/Google and validated project auth-domain origins empirically required by the chosen popup flow when cloud mode is configured; validate the auth domain before interpolating it and add no wildcard. Preserve production `unsafe-inline` residual documentation unless a separately proven nonce/hash migration is feasible without breaking static/demo behavior. Create a non-production browser/emulator configuration that never targets real Firebase. Cover guest with Firebase unset, sign-in success, cancellation, popup block/provider failure, import/merge, signed refresh restoration, sign-out, deletion failure/success, cloud timeout/quota-shaped failure, assessment success with persistence failure, configuration-off rollback, focus order, accessible names/status, axe, and 1440x900/390x844 overflow. Assert challenge/report/fallback behavior and local progress survive every failure.</action>
  <verify>`npm run test:e2e`; new non-watch Firebase browser command; `npm run build`; `npm run security:bundle`; inspect production CSP in `npm run smoke:production`.</verify>
  <done>The optional identity surface is accessible and production-shaped without widening CSP or weakening the guest/fallback path.</done>
</task>
</tasks>

<verification>
- In cloud-off mode, bundle/start/dashboard/primary workflow match the local baseline.
- In emulator mode, signed progress restores across clean browser contexts and cross-user data never appears.
- Auth cancellation/failure and persistence outage produce local status, not lost work or blocked submission.
</verification>

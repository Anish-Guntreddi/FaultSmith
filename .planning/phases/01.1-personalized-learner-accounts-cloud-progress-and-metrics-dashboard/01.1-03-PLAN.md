---
phase: 01.1-personalized-learner-accounts-cloud-progress-and-metrics-dashboard
plan: 03
type: execute
wave: 2
depends_on: [01.1-01, 01.1-02]
files_modified:
  - src/lib/contracts.ts
  - src/lib/progress-contracts.ts
  - src/server/firebase-admin.ts
  - src/server/firebase-identity.ts
  - src/server/firebase-identity.test.ts
  - src/server/progress-repository.ts
  - src/server/progress-service.ts
  - src/server/progress-service.test.ts
  - src/app/api/progress/route.ts
  - src/app/api/progress/routes.test.ts
  - src/app/api/challenges/assess/route.ts
  - src/app/api/challenges/routes.test.ts
  - tests/firebase/progress.emulator.test.ts
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04, CLOUD-01, CLOUD-02, PRIV-01, SEC-03, QA-03]
must_haves:
  truths:
    - The server derives UID only from a verified Firebase token, rejects unverified email/password identities for cloud routes, and no client value can select another user's document path.
    - Deterministic assessment completes before persistence; only verified evidence completes a lesson and cloud failure never invalidates the report.
    - Read/import/delete and assessment writes are strict, idempotent, capped, no-store, cross-user isolated, and content-minimal.
  artifacts:
    - Server-only lazy Admin/identity DAL, Firestore repository/service, authenticated progress route, and assessment persistence hook.
    - Fake-service and emulator integration coverage for identity, isolation, replay, retention, deletion, and degraded operation.
  key_links:
    - Public Route Handlers re-authorize at the data boundary; Firebase Admin and credentials cannot enter client-safe modules.
    - Server maps challenge/project/skill to the approved lesson; it never accepts a completion step or UID as authority from the client.
---

<objective>
Implement the authenticated server persistence boundary and bind cloud progression to FaultSmith's existing deterministic assessment authority.
</objective>

<tasks>
<task id="01.1-03-01" type="auto">
  <name>Build the server-only Firebase identity DAL and bounded progress repository</name>
  <files>src/server/firebase-admin.ts, src/server/firebase-identity.ts, src/server/firebase-identity.test.ts, src/server/progress-repository.ts, src/server/progress-service.ts, src/server/progress-service.test.ts, src/lib/progress-contracts.ts</files>
  <action>Follow the bundled Next.js 16 DAL/DTO guidance. Mark all privileged modules `server-only`. Initialize Admin lazily only when the complete cloud feature/server configuration exists; validate the in-memory credential shape and emit no value or provider detail. Parse a single bounded Bearer Authorization value, reject missing/duplicate/mixed-scheme/oversized/malformed input on protected routes, verify the Firebase ID token for the configured project, reject password-provider identities whose verified token does not confirm email verification, and return only an internal UID wrapper. Google and password identities use the same wrapper/profile path; provider/email request fields cannot select or merge a profile. Create a repository whose methods accept the verified identity wrapper—not raw client strings—and construct the fixed Firestore path internally. Implement strict DTO reads, monotonic import with `local_import`, deterministic assessment persistence with `server_verified`, SHA-256 idempotency material that stores no learner content, a 50-attempt cap with explicit oldest deletion, and same-user data deletion. Use dependency injection for fast fake tests. Collapse Firebase errors into safe availability/auth outcomes; never log password, token, UID, email, document path, credential, DTO, or provider response.</action>
  <verify>`npx vitest run src/server/firebase-identity.test.ts src/server/progress-service.test.ts`; `npm run lint`; `npm run typecheck`; `npm run security:source`.</verify>
  <done>All cloud data access is centralized, server-only, identity-derived, bounded, idempotent, and privacy-minimal.</done>
</task>

<task id="01.1-03-02" type="auto">
  <name>Add progress routes and persist authoritative assessment outcomes</name>
  <files>src/lib/contracts.ts, src/app/api/progress/route.ts, src/app/api/progress/routes.test.ts, src/app/api/challenges/assess/route.ts, src/app/api/challenges/routes.test.ts, tests/firebase/progress.emulator.test.ts</files>
  <action>Add strict public schemas for cloud progress DTOs and a bounded optional assessment sync status that cannot change completion/test authority. Implement authenticated `GET`, bounded one-time import `POST`, and explicit deletion `DELETE` for `/api/progress` (or equally bounded subroutes), all same-origin, rate-limited, no-store, exact-key, and verified through the identity DAL. For assessment, treat identity as optional because the challenge route remains public: after `assessChallengeWorkflow` decides the exact response, derive the approved lesson server-side from the challenge registry and persist the bounded summary only for a valid verified identity; invalid, unverified, or unavailable identity or Firestore failure produces no write and only a bounded local/degraded sync fact while returning the authoritative assessment. Never accept UID, email, password, provider, lesson completion, score, status, source, or Firestore path from a progress-write client. Add route adversarial tests plus emulator tests for unverified denial, verified password/Google same-contract success, clean cross-process restoration, two-user isolation, replay/concurrency, retention, direct document inference, deletion, failed-assessment non-completion, import provenance, and Firestore unavailability.</action>
  <verify>`npx vitest run src/app/api/progress/routes.test.ts src/app/api/challenges/routes.test.ts`; `npm run test:firebase`; `npm test`; `npm run build`; `npm run security:bundle`.</verify>
  <done>Protected progress APIs fail closed, public assessment remains reliable, and cloud completion is objectively tied to verified server evidence.</done>
</task>
</tasks>

<verification>
- Prove user A cannot read/import/delete user B and no client field influences UID/path/completion authority.
- Replaying identical assessment creates one attempt; failing assessment creates no completion.
- Disable/misconfigure Firebase and prove the same assessment/report/local progress still succeed.
</verification>
